# bh-VLA: Unified Vision-Language-Action Model Training Framework
# Requirements for training ACT and pi0.5 VLA policies

# ---- Core ML / Deep Learning ----
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
numpy>=1.24.0

# ---- Data Loading / Processing ----
h5py>=3.10.0
pillow>=10.0.0
opencv-python>=4.8.0
tqdm>=4.65.0
pyyaml>=6.0

# ---- Optional: RLDS / TensorFlow-Datasets (for RLDS format datasets) ----
tensorflow-datasets>=4.9.0
tensorflow>=2.13.0

# ---- Optional: Robot Hardware (SO-101 / ALOHA) ----
pyserial>=3.5
lerobot>=0.1.0

# ---- Optional: Visualization ----
matplotlib>=3.7.0
wandb>=0.15.0

# ---- Optional: Mixed Precision ----
# Included in PyTorch (torch.cuda.amp) — no extra package needed

# ---- Optional: Model Serving / Export ----
# onnx>=1.14.0
# tensorrt>=8.6.0

# ---- Development / Testing ----
pytest>=7.0
black>=23.0
isort>=5.12
flake8>=6.0
